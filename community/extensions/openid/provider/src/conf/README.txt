This directory contains properties files that should be tuned to match your
deployment. It also contains an example of ldif file to add the OpenID attribute
to OpenSSO's embedded store (see top level README.txt file for additional
information).
