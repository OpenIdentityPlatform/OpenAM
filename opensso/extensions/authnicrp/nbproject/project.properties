
application.desc=Information Card authentication module for OpenSSO
application.title=Authnicrp
application.vendor=Patrick Petit
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Authnicrp.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.amserver.jar=extlib/amserver.jar
file.reference.opensso-sharedlib.jar=extlib/opensso-sharedlib.jar
file.reference.servlet.jar=extlib/servlet.jar
file.reference.xmldap-1.0.jar=extlib/xmldap-1.0.jar
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.servlet.jar}:\
    ${file.reference.xmldap-1.0.jar}:\
    ${file.reference.opensso-sharedlib.jar}:\
    ${file.reference.amserver.jar}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked
javac.deprecation=false
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jnlp.codebase.type=local
jnlp.codebase.url=file:/home/ppetit/workspace/opensso/extensions/authnicrp/dist/
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false

main.class=
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.licences.dir=licences
src.source.dir=source
