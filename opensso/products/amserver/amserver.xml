<project name="amserver">
    <dirname property="amserver.basedir" file="${ant.file.amserver}"/>
    <property file="${amserver.basedir}/amserver.properties"/>
    <property file="${amserver.basedir}/tracker.properties"/>
</project>
