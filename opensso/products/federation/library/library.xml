<project name="library">
    <dirname property="library.basedir" file="${ant.file.library}"/>
    <property file="${library.basedir}/library.properties"/>
    <property file="${library.basedir}/tracker.properties"/>
</project>
